# adr/0026: the public edge for chat.reserve-me.ru, auth.reserve-me.ru, console.reserve-me.ru,
# grafana.reserve-me.ru, and (as of 8-02) demo-shop1.reserve-me.ru. Same NGINX Gateway Fabric
# component as overlays/local/ (adr/0014) - only the listeners, hostnames, and TLS wiring differ.
# demo-shop2.reserve-me.ru stays unrouted here: it has no backend (adr/0026's subdomain table names
# it reserved, not built - 8-02's own scope note declines building a second demo tenant/page), and
# an HTTPRoute with no real Service behind it would only be a route this item could not honestly
# claim was verified. grafana.reserve-me.ru is different - `grafana` (k8s/base/grafana.yaml, `7-03`)
# is a real, already-deployed Service, so this route is real and verifiable, not speculative like
# console's own entry below. Author's own call (2026-08-24): public Grafana over an SSH tunnel, for
# reachability without a live session - gated by Grafana's own login (GRAFANA_ADMIN_PASSWORD, a real
# generated secret, not the local overlay's throwaway value) plus TLS, not by network topology alone.
#
# console.reserve-me.ru's own listener/HTTPRoute below were designed by 8-01 ahead of this item -
# 8-02 only had to create the `ago-console` Service (console-static.yaml) the existing backendRef
# already names; nothing in this file needed to change for that route to start working.
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: ago-chat-gateway
spec:
  gatewayClassName: nginx
  listeners:
    # Plain HTTP stays open on every host in this overlay - cert-manager's own gatewayHTTPRoute ACME
    # solver (adr/0026, tls.yaml) answers Let's Encrypt's HTTP-01 challenge through a temporary
    # HTTPRoute it creates itself under this same Gateway, and it needs a working :80 listener to do
    # that. Left un-redirected deliberately: a permanent HTTP->HTTPS redirect is a real, small
    # addition (Gateway API's RequestRedirect HTTPRoute filter) but this item has no live server to
    # verify it against, and an unverified redirect rule is worse than an honestly-absent one.
    - name: http
      port: 80
      protocol: HTTP
      hostname: "*.reserve-me.ru"
    - name: https-chat
      port: 443
      protocol: HTTPS
      hostname: chat.reserve-me.ru
      tls:
        mode: Terminate
        certificateRefs:
          - name: ago-public-tls
    - name: https-auth
      port: 443
      protocol: HTTPS
      hostname: auth.reserve-me.ru
      tls:
        mode: Terminate
        certificateRefs:
          - name: ago-public-tls
    # console-static.yaml (8-02) creates the ago-console Service this listener's own HTTPRoute
    # (below) already names as its backendRef - designed by 8-01 ahead of the Service actually
    # existing, now real and servable end to end.
    - name: https-console
      port: 443
      protocol: HTTPS
      hostname: console.reserve-me.ru
      tls:
        mode: Terminate
        certificateRefs:
          - name: ago-public-tls
    - name: https-grafana
      port: 443
      protocol: HTTPS
      hostname: grafana.reserve-me.ru
      tls:
        mode: Terminate
        certificateRefs:
          - name: ago-public-tls
    # 8-02: demo-shop1-static.yaml creates the ago-demo-shop1 Service this route's own backendRef
    # names - the public demo landing page + the widget's own built bundle, served together
    # (demo-shop1-static.yaml's own header comment has the same-origin reasoning).
    # demo-shop2.reserve-me.ru stays unrouted (adr/0026: reserved, not this item's own scope).
    - name: https-demo-shop1
      port: 443
      protocol: HTTPS
      hostname: demo-shop1.reserve-me.ru
      tls:
        mode: Terminate
        certificateRefs:
          - name: ago-public-tls
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: ago-chat-api
spec:
  parentRefs:
    - name: ago-chat-gateway
      sectionName: https-chat
  hostnames:
    - chat.reserve-me.ru
  rules:
    # Same three prefixes as overlays/local/gateway.yaml - unchanged by the public/TLS shift, only the
    # hostname and parent listener differ.
    - matches:
        - path:
            type: PathPrefix
            value: /healthz
      backendRefs:
        - name: ago-chat-api
          port: 80
    - matches:
        - path:
            type: PathPrefix
            value: /api
      backendRefs:
        - name: ago-chat-api
          port: 80
    - matches:
        - path:
            type: PathPrefix
            value: /hubs
      backendRefs:
        - name: ago-chat-api
          port: 80
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: ago-chat-auth
spec:
  parentRefs:
    - name: ago-chat-gateway
      sectionName: https-auth
  hostnames:
    - auth.reserve-me.ru
  rules:
    # The whole app, not a path prefix - Keycloak owns this entire hostname (its login UI, its
    # admin console, its OIDC discovery/JWKS/token endpoints all live under one root here).
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: keycloak
          port: 8080
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: ago-console
spec:
  parentRefs:
    - name: ago-chat-gateway
      sectionName: https-console
  hostnames:
    - console.reserve-me.ru
  rules:
    # ago-console Service: console-static.yaml (8-02), port 80 matching every other Service in
    # this overlay's own convention (base/*.yaml).
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: ago-console
          port: 80
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: ago-demo-shop1
spec:
  parentRefs:
    - name: ago-chat-gateway
      sectionName: https-demo-shop1
  hostnames:
    - demo-shop1.reserve-me.ru
  rules:
    # ago-demo-shop1 Service: demo-shop1-static.yaml (8-02) - the whole app, not a path prefix,
    # same shape as ago-console above (a single static site, no API path split needed here).
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: ago-demo-shop1
          port: 80
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: grafana
spec:
  parentRefs:
    - name: ago-chat-gateway
      sectionName: https-grafana
  hostnames:
    - grafana.reserve-me.ru
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: grafana
          port: 3000
---
apiVersion: gateway.nginx.org/v1alpha1
kind: UpstreamSettingsPolicy
metadata:
  name: ago-chat-api-least-conn
spec:
  targetRefs:
    - group: core
      kind: Service
      name: ago-chat-api
  # Same reasoning as overlays/local/gateway.yaml (edge.md): least_conn, not round-robin, for
  # long-lived WebSocket connections.
  loadBalancingMethod: least_conn
